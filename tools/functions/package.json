{
  "name": "charity-recon-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Charity_Recon: sync memberIds with members map and backfill.",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
